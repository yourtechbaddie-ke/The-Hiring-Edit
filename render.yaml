services:
  - type: web
    name: the-hiring-edit
    runtime: static
    plan: free
    buildCommand: echo "Static site — no build step required"
    staticPublishPath: .
    previews:
      generation: automatic
